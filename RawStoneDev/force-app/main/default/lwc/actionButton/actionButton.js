import { LightningElement } from 'lwc';

export default class ActionButton extends LightningElement {
    handleAction1() {
        // Logica per Azione 1
    }

    handleAction2() {
        // Logica per Azione 2
    }
}